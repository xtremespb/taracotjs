#!/usr/bin/env node
var app = require('../app');
var config = require('../config');

app.set('port', config.port || process.env.PORT || 3000);

var server = app.listen(app.get('port'), function() {
  console.log('TaracotJS server listening on port: ' + server.address().port);
});
